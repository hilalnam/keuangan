services:
  - type: web
    name: keuangan-backend
    env: node
    plan: free
    root: apps/backend
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: ALLOW_ANONYMOUS
        value: true
      - key: DEV_USER_ID
        value: dev-user
      - key: BETTER_AUTH_SECRET
        generateValue: true
      - key: BETTER_AUTH_URL
        fromService:
          type: web
          name: keuangan-backend
          property: host
